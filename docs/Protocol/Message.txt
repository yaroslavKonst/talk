Message structure
#################
#################

Point to point message
######################

| P2P_MESSAGE | encryption scheme |            \
| source address | source key |                |
| destination address | destination key |      | message
| timestamp | index |                          | header
| message thread id |                          |
| message body size | nonce |                  /
| encrypted message body |                       message body

Since the message body can be quite big, it is proposed to divide it
into blocks to enable incremental encryption and decryption. So, the
encrypted message body is stored as a block sequence.

Block structure:
| block size | MAC | encrypted payload |

Each MAC protects the header and its message body block to ensure
consistency. The full message size verification is done using the
message body size field in the header.

Group message
#############

Groups are maintained as entities separated from users. Any user can
create a group on the server where the user is registered and then invite
users from any servers.

Any group has its own name.

| GROUP_MESSAGE | encryption scheme |          \
| source address | source key |                |
| group name | group keys |                    | message
| timestamp | index |                          | header
| message thread id |                          |
| message body size | nonce |                  /
| encrypted message body |                       message body

Encrypted message body has the same structure as in point to point messages.

The group keys field is an array of key entries.
| entry number | entry 1 | ... | entry N |

Each entry has the following structure:
| destination key | MAC | nonce | encrypted symmetric key |

The idea of group message structure is the following: the message is
encrypted with a one-time symmetric key. Then this key is encrypted
by public keys of message receivers. It forms an array of group keys,
each entry allows the appropriate receiver to decrypt the message.

MAC from the group keys entry protects its entry.

So, to decrypt the message the message receiver finds an entry with its
public key, checks the entry and decrypts the symmetric key. Then the
receiver decrypts the message body using decrypted symmetric key.

Service messages
----------------

The server may send a message where sender name is equal to the group name
and group keys field contains keys of the message destinations. These messages
contain commands and notifications from the server to group members.

Server command list:
| NOTIFY_ADD | name of new user | name of invitation sender |
Notifies the user that the user was added to the group.

| NOTIFY_REMOVE | name of removed user | name of removal initiator |
Notifies the user that the user was removed from the group.

| NOTIFY_CAPABILITY_CHANGE | capabilities |
Notifies the user that the assigned capabilities have been changed.

Message body structure
######################

Message body is a sequence of entries.
Entry structure:
| entry type (8 bit integer) | entry length (64 bit integer) | data |

There can be multiple entries of the same type in the sequence.

Entry types
-----------
This section describes contents of the "data" field from the previous
subsection.

TEXT -- text in UTF8 encoding without BOM.
"data" is a text, text length is obtained from the entry size.

ATTACHMENT -- any data, byte array.
"data" structure:
| attachment name (string) | attachment payload |
Payload size can be derived from entry size and attachment name length.

KEY -- new public key. It can be sent if the user wants to notify others
       about upcoming key change.
| user name (string) | key type (32 bit integer) | key (32 bytes) |

EMPTY -- special entry type with random payload. Encryption hides message
contents but not message size. This entry can be used to increase message
size by random value and hide the actual payload size. Must be skipped during
parsing.

This list is not exhaustive and finalized. Client extensions may define
other types. Clients must mark such entries as unsupported and continue
parsing.
